# glyph-game

## description of project 

Our planned project is to have a Glyph game, which is a fun visual twist on wordle. The user will first be shown a single abstract shape comprised of 4-7 letters. They must then must guess the correct letters included in the shape, and then rearrange the letters to match the secret correct word. 

## 3 functions

get_word() : retrieve either a 4, 5, 6, or 7 letter word from a word bank that we will create in a JSON file

initialize_game() : create the grid/board, set starting score/number of correct letters guessed

get_response() : this function will assess the user's input and decide whether to return "correct", and keep a letter in the display, or "incorrect" if the letter is not correct, and don't keep the letter


## task delegation

Pihu's job: Write the return code (get_response()) and figure out code logic for keeping score of correct letters and ending the game when score meets the threshold

Liv's job: Figure out the proper code for setting up the graphics, write the get_word() code after creating json file with Pihu of random words 

## group members
Group members: 

Olivia Koller (okoller2)

Pihu Chawra (pchawra2) 


## updated changes: 
Material we will provide to users is all the letters of the alphabet as submission options as well as the amount of letters that are in the glyph
The logic of our program is that we have letters that all fit within a certain boundary to form a combined symbol. The user will enter a letter, and the computer will then assess if that response is identical to one of the letters in the randomly chosen word and keep it to show the user that while the rest of the word may be incorrect, a single letter is correct.
Someone would use this program for enjoyment or to continually train their brain 
Datafile would be a python file with imported json file (collection of words) and imported text file (instructions) 
To test the entire functionality of the program, the user would purposely test words that they know have one letter correct to make sure that the computer recognizes single letters and also recognizes those that are incorrect

## updated changes project check-in 3:
We reordered some of the functions like def normalize_segment in order to reduce the number of nameerrors and have the code run in a chronological order that made sense. We have also been renaming some of our functions, so below I have an updated version of our main functions for clarity:

Reordered functions: 
def _normalize_segment(coords): 
this makes a segment so that (a,b) -> (c,d) and any other letter combination appears the same size based on standard coordinates established earlier in the code (width=600, height=600)
def get_glyph(word):
this is the same as the function we previously called get_word(), except it is retrieving uniquely shaped letters that we designed based on coordinates within the hypothetical letter box 
def draw_letter(canvas, letter, x, y, size):
this function creates the letter on the gui 
def _pick_daily_word(self): 
this function ensures users get a random and non repeating word every time
class GlyphGame:
this class actually dictates the color of the different buttons and titles that will appear as the user makes choices. 
def _draw_glyphs(self): 
this automatically makes the game draw letters within the canvas and also ensures that if the user wants to start a new trial of the game, the previous trial doesn't stay 

Measures of Success: As of right now, the game isn't fully complete, but we are at a checkpoint of progress, so our way for graders to know our code is being executed properly by seeing a small window pop-up with a random letter appear on the screen. The letter should be a different one each time that the grader runs the code. It is worth noting that while we have written code related to buttons and interactions, we are still working out the kinks and thus we made some of those comments instead to avoid interfering with the code's ability to run properly as a prototype. 

Overall Methods:

Created custom/stylized lettering by making an invisible box wherein letters are confined and drawn based on a set of coordinates that we calculated
Created stylized color palette for all text that appears for the user
Created an immense word bank of 5 letter words
Want users to be able to input letters and see their guessed letters register on the screen
Want users to enter their guesses and recieve a notification indicating what letters were guessed correctly (which will remain on the screen) and what letters were guessed incorrectly (which will disappear)
Want users to see when they've guessed the entire word
Created an end result where the winner is placed back at the home page and a completely new and random word can be utilized for the next trial/round




